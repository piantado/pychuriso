
# What happens if our base facts are slightly mis-specified?
# so that the thing we learn is at a slightly higher level?

True  := (K K)
False := K

unique dangerous small

True  = (dangerous a)
False = (dangerous b)

True  = (small a)
False = (small b)

# Do I learn that dangerous things are big?
True = (small x)

show (dangerous x)